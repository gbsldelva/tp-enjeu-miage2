[project]
name = "tp-enjeu-miage2"
version = "0.0.1"
description = "Tp ordonnancement des tâches"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
]
